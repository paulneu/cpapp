import { EntityType } from '@sap-ux/vocabularies-types';
import { FileData } from '../../common';
import { ExtensionLogger } from '../../../v2';
/**
 * Generates an app specific schema out of the generic schema.
 * Generic types are replaced by information from the app specific annotations.
 *
 * @param genericSchema - generic JSON schema of a list report
 * @param entityTypeName - the base entity type of the given page
 * @param entityTypes - list of all entity types
 * @param page - page as defined in the manifest
 * @param manifest - manifest.json of the app
 * @param {ExtensionLogger} logger - Logger class for logging messages
 */
export declare function generateAnalyticalListReportSchemaV2(genericSchema: object, entityTypeName: string, entityTypes: EntityType[], manifest?: object, fragments?: FileData[], logger?: ExtensionLogger): object;
