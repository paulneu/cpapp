import { ConverterOutput } from '@sap-ux/vocabularies-types';
import { ExtensionLogger } from '../../../v4';
/**
 * Generates an app specific schema out of the generic schema.
 * Generic types are replaced by information from the app specific annotations.
 * @param genericSchema  - generic JSON schema of an object page
 * @param entityTypeName - the base entity type of the given page
 * @param {ConverterOutput} oDataServiceAVT  - complete service information, as returned by annotation vocabularies tool
 * @param manifest - manifest.json of the app
 * @param {ExtensionLogger} logger - Logger class for logging messages
 *
 * @returns the app specific JSON schema
 */
export declare function generateObjectPageSchemaV4(genericSchema: object, entityTypeName: string, oDataServiceAVT: ConverterOutput, manifest: object, logger?: ExtensionLogger): object;
